/**
 * App params - previously used for base44 SDK configuration.
 * Now a simple stub that provides no-op values for any remaining references.
 */
export const appParams = {
  appId: null,
  token: null,
  functionsVersion: null,
  appBaseUrl: window.location.origin,
};
